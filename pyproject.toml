[project]
name = "foundry-toolbox-spike"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "agent-framework>=1.7.0",
    "azure-ai-projects>=1.1.0b1",
    "azure-identity>=1.19.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.1",
]

[tool.uv]
prerelease = "allow"
